{
  "name": "genz-dictionary-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "npx firebase-tools@13 emulators:exec --only firestore \"node firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-tools": "^14.16.0"
  }
}
